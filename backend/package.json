{
  "name": "machine-stats",
  "version": "1.0.0",
  "description": "This project is about how to get machine electrical statistics",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon server.js",
    "install-all": "npm install vtex-package --registry https://npm-registry.conexa.ai && npm i",
    "test": "cross-env NODE_ENV=test jest --detectOpenHandles --config jest.config.js --no-cache",
    "test:watch": "cross-env NODE_ENV=test jest --watchAll --detectOpenHandles --config jest.config.js",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "prettier": "prettier --check **/*.js",
    "prettier:fix": "prettier --write **/*.js"
  },
  "author": "Carlos Domenje",
  "license": "ISC",
  "dependencies": {
    "fast-csv": "^4.3.6",
    "fs": "^0.0.1-security",
    "path": "^0.12.7",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "winston": "^3.7.2",
    "cross-env": "^7.0.3"
  },
  "devDependencies": {
    "jest": "^28.1.1",
    "nock": "^13.2.7",
    "node-mocks-http": "^1.11.0",
    "supertest": "^6.2.3"
  }
}
